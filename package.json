{
  "name": "@tristar/json-migrate",
  "version": "0.1.0",
  "description": "A JSON migration tool that also supports rollback (by backing up all target files before migration starts).",
  "main": "index.js",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1",
    "type-check": "tsc --noEmit",
    "format": "prettier --write ./src/**/*.ts"
  },
  "keywords": [
    "json",
    "migration"
  ],
  "author": "tristar",
  "license": "MIT",
  "dependencies": {
    "typescript": "^5.0.4"
  },
  "devDependencies": {
    "prettier": "^2.8.8"
  }
}
